// src/data/shinyweekly.loader.js
// Shiny Weekly data loader
//
// INTERNAL DATA CONTRACT — SHINY WEEKLY ROW (CI-OWNED)
//
// Each entry in shinyweekly.json.data[] has the following shape:
//
// {
//   week: string                  // stable week identifier
//   week_label: string            // human-readable week label
//   date_start: string            // ISO date string (YYYY-MM-DD)
//   date_end: string              // ISO date string (YYYY-MM-DD)
//   ot: string                    // original trainer / hunter name (lowercase)
//   pokemon: string               // canonical pokemon key
//   method: string | null         // hunt method
//   encounter: number | null      // encounter count, if applicable
//   secret: boolean               // secret shiny flag
//   alpha: boolean                // alpha shiny flag
//   run: boolean                  // run-away shiny flag
//   lost: boolean                 // lost shiny flag
//   clip: string | null           // video clip URL
//   notes: string | null          // freeform notes
// }
//
// Guarantees:
// - All listed fields are always present
// - Boolean fields are never undefined
// - Nullable fields may be null but are never missing
// - Rows are ordered as generated by CI
// - No runtime normalization or reshaping occurs

import { loadJson } from './json.loader.js';

export async function loadShinyWeekly() {
  return loadJson('/data/shinyweekly.json', 1);
}
